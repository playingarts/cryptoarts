/**
 * Contract Resolvers
 */

// Note: Contract queries are currently commented out in the original schema
// but the getContract and getContracts functions are used by other resolvers

export const resolvers: GQL.Resolvers = {
  // Query: {
  //   contract: (_, args) => getContract(args),
  // },
};
